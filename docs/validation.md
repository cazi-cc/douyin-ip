# 本地验证记录

构建日期：2026-09-06。版本：0.1.0。

本文件记录当前本地交付的检查结果，不代表所有移动客户端已进行真机导入。

## 已通过

| 检查 | 结果 |
| --- | --- |
| 统一规则生成 | 23 份配置/脚本产物 + 1 份 SHA-256 清单，与源文件一致 |
| Python 测试 | 10 项通过，含 11 种适配 × 18 个主机场景的语义比较 |
| JavaScript 扩展 | 原配置不变、前插顺序、重复执行、对象键顺序变化、冲突拒绝均通过 |
| 文档链接 | 所有相对文件链接目标存在 |
| 公开内容扫描 | 覆盖首页、文档、配置、规则源、工具、测试及 CI；未发现检查模式中的私人地址/节点/凭据 |
| Mihomo | Meta v1.19.30，原生 `-t` 配置检查通过 |
| Xray | 26.3.27，原生 `run -test` 配置检查通过 |
| sing-box | 1.14.0，原生 `check` 通过，两个 source 规则集编译通过 |

测试包含：音乐例外先于 API、API 候选仍代理、域名后缀边界、无关主机继续原规则；合并不覆盖原输入、不丢失旧规则/出站/DNS/TUN、保留 sing-box 前处理、拒绝锁定规则和被修改的同名项目规则。

原生检查采用合成的回环测试配置，只执行检查/编译，没有启动代理服务或连接实际代理节点。官方二进制从各项目对应版本 Release 下载，下载文件 SHA-256 与 GitHub Release 元数据的 digest 核对一致；二进制保留在本机工具缓存，不随项目分发。

## 未宣称通过

- Surge、Loon、QX、Stash、Mihomo 图形客户端、v2rayNG、v2rayN、sing-box 手机客户端的全流程真机导入与公开属地验收。
- 当前新生成 Shadowrocket 模块在每个版本的导入兼容性。
- 原始文件 URL 在各客户端中的在线安装验收。GitHub Actions 的在线执行结果以仓库 Actions 页面为准；本表记录本地检查。
- 所有账号、节点、地区或未识别流量的长期安全与属地保证。

维护者已有的 Shadowrocket / PassWall2 使用证据单列于[实测依据](evidence.md)。

## 复现

见[本地构建](development.md)。提交规则变更后重新运行检查，不把本文结果当作后续版本自动通过的证明。
