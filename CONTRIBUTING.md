# 参与维护

欢迎反馈真实可复现的域名命中与客户端兼容问题。请先阅读[技术说明](docs/technical.md)和[反馈要求](docs/faq.md#反馈问题要提供什么)。

新增规则需要说明具体触发操作、命中策略、用途依据、体验与公开属地对照。不要只根据名字像媒体、IP 在国内、域名曾出现在其他规则库中就加入直连。

修改流程：改 `source/rules.json` → 重新生成 → 执行构建检查与测试 → 更新证据与版本记录。API 后缀兜底、媒体优先和其他流量不受影响是基本约束。关键词变更需要额外检查匹配范围。

不接受：完整订阅、真实节点、Token、Cookie、账户身份、原始全屋日志，或没有证据的“绝对安全”“所有地区都成功”等表述。

客户端适配应使用该软件真实支持的格式，注明版本和验证层级；不要把 JSON/YAML 能解析等同于手机导入成功。
